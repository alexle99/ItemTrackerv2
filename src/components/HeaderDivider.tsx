import { Box } from '@mui/material';

export const HeaderDivider = () => {
  return (
    <Box
      sx={{
        borderBottom: '.1rem grey solid',
        marginLeft: '.5rem',
        marginRight: '.5rem',
      }}
    />
  );
};
